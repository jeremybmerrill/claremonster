<% provide(:title, 'Current Seeds') %>
<%= link_to 'Plant a new seed', new_wish_path, class: "btn btn-primary btn-large" %>

<% @wishes.each do |wish| %>
<%= render "wishinalist", wish: wish %>
<% end %>
